// this will always generate a number between 0 and 1 only
console.log(Math.random());

// Math.floor truncates everything after the decimal
// random # between 1 and 6
console.log(Math.floor(Math.random() * 6) + 1); // add 1 so you don't get 0